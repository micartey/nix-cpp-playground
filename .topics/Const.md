## Constants

In C++ there is a distinction between `const` and `constexpr`:

- `const` is a variable that cannot be modified after initialized. Its value can be determined at **runtime**.
- `constexpr` is a variable where the value is known at **compile time**
